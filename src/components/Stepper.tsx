import { useAppState } from '@state/AppState';

const labels = ['Mode', 'Meals', 'Desserts', 'Pantry', 'Restock', 'Final'];

export function Stepper() {
  const { state } = useAppState();
  return null;
}


